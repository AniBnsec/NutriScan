{
  "name": "nutriscan",
  "version": "1.0.0",
  "description": "AI Portion & Nutrition Scanner",
  "scripts": {
    "start": "npm start --prefix server",
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "install-all": "npm install --prefix server && npm install --prefix client",
    "dev": "npm run install-all && npm run dev --prefix server & npm run dev --prefix client"
  }
}
